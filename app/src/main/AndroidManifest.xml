<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unsoed.responsi1mobileh1d023100">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Responsi 1 Mobile H1D023100"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Responsi1MobileH1D023100">

        <!-- Tambahkan android:exported="true" di sini -->
        <activity
            android:name="com.unsoed.responsi1mobileh1d023100.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".CoachActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".TeamSquadActivity" />
    </application>

</manifest>
